global.show_box = false; // start hidden

// creates variable used in the draw gui step for creating the scrolling of the background
scroll_offset = 0;

// creates the input for text when answering a math question
user_text = "";
keyboard_string = "";